[project]
name = "w3-ex2-part1-registry-tax"
version = "0.1.0"
description = "Week 3 Ex.2 Part 1 — measuring the MCP registry tax in prompt_tokens"
license = "CC-BY-NC-SA-4.0"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "mcp>=1.14,<1.15",
]
