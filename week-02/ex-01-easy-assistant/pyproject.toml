[project]
name = "easy-assistant"
version = "0.1.0"
description = "Week 2 · Exercise 1 — EASY-CHATGPT grows its first (single-file) RAG"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "openai>=1.30",
    "python-multipart>=0.0.9",
]
